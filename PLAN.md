# 작업 계획

기능 상세는 [REFERENCES.md](./REFERENCES.md) 참고.
REFERENCES.md §2의 P0/P1/P2는 **기능 우선순위**, 아래 P0~P3는 **작업 단계**. 축이 다르다.

**백엔드는 전 단계에서 0.** 관리 사이트는 나 혼자(+지인) 쓰는 도구라 계정·서버가 필요 없다.
편집은 브라우저에서, 보관은 IndexedDB, 산출물은 `config.json` + 이미지 zip, 배포는 정적 호스팅.
(RSVP·방명록을 붙일 때도 서버 말고 Google Apps Script + 스프레드시트 — REFERENCES.md §3)

---

## P0 — 템플릿 1개 완성 (정적)

REFERENCES.md §1 레이아웃 + §2 "P0 — 채택" 기능. 이미지·지도 전부 정적.

- `src/config.ts` 하나로 제어 (§6 스키마). **템플릿 1개짜리 스키마로 충분하다. 다중 템플릿 구조는 P2에서 보완**
- 섹션: cover / greeting / gallery / calendar / location / account / share
- 이미지: `public/` 에 직접 넣음. 업로드 없음
- 지도: 정적 이미지 + 네이버/카카오/구글 딥링크 버튼 (SDK 없음)
- 배경 애니메이션: §5 canvas 140줄, `"petal" | "snow" | null`
- RSVP·방명록 없음

스택: React + TS + Vite. 레퍼런스 대부분이 이 조합이고 P1과 코드를 공유한다.

**완료 기준**: config만 고쳐서 다른 커플 청첩장이 나온다. 실기기(iOS 카톡 인앱브라우저) 확인.

---

## P1 — 관리 사이트 (P0와 별개 단계)

P0 템플릿을 폼으로 편집 → 실시간 미리보기 → 내보내기.

- 페이지 애니메이션 (섹션 진입 효과)
- 이미지 업로드 / 배경 업로드 — File API로 로컬 선택, IndexedDB 보관
- 지도: 네이버 · 카카오 · 구글 선택
- 섹션별 배경 포맷
- 섹션별 세부 커스텀

이 시점에 P0의 config 스키마에서 실제로 부족했던 게 드러난다. 그걸 보고 고친다.

---

## P2 — 확장 (아직 미계획)

템플릿 추가 / 편집 / 리소스 저장소 등. **세부 스펙은 P0 완료 후 차차 계획한다.**

다중 템플릿 구조도 여기서 잡는다. 템플릿이 2개가 되고 나서야 "템플릿 공통 필드"와
"템플릿 고유 필드"의 경계가 보인다. P0·P1에서 미리 나누면 틀린 선을 긋는다.

---

## P3 — 배포

정적 호스팅. 청첩장 1건 = 빌드 1개. 업체는 **미정** (아래 참고).

---

## 지금 열려있는 결정

**1. 템플릿 소스가 될 레퍼런스 1개 선정** (REFERENCES.md §7 오픈소스 목록 중)

**2. 배포 업체 — 보류.** 순수 정적이라 셋 다 충분하고, 나중에 갈아타는 비용도 거의 0.

- **Cloudflare** — 추천. 대역폭 무제한(사진 많은 페이지에 안전), 서울 PoP, 상업용 제약 없음.
  신규 프로젝트는 Pages 대신 Workers 정적 자산 쪽으로 유도됨
- **Vercel** — 계정 보유. Hobby 플랜은 **약관상 비상업용 전용** — 나중에 돈 받고 만들면 위반
- **Netlify** — 미사용. Drop(폴더 드래그 배포)이 "1건 = 빌드 1개" 흐름과 잘 맞음

무료 티어 수치는 자주 바뀌므로 배포 시점에 재확인.
